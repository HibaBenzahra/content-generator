services:
  content-generator:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./pipeline:/app/pipeline
      - ./main.py:/app/main.py
    restart: unless-stopped
